---
layout: base.njk
title: Etusivu
lang: fi
translationKey: home
---

{% set uniquePresentationKeys = [] %}
{% for item in ((canva and canva.tableRows) or []) %}
  {% set key = ((item.url or "") ~ "|" ~ (item.title or "")) | trim %}
  {% if key != "|" and (uniquePresentationKeys.indexOf(key) == -1) %}
    {% set _ = (uniquePresentationKeys.push(key), null) %}
  {% endif %}
{% endfor %}
{% for item in (collections.presentations or []) %}
  {% set key = (((item.data and item.data.url) or "") ~ "|" ~ ((item.data and item.data.title) or "")) | trim %}
  {% if key != "|" and (uniquePresentationKeys.indexOf(key) == -1) %}
    {% set _ = (uniquePresentationKeys.push(key), null) %}
  {% endif %}
{% endfor %}
{% for item in ((finnaAoe and finnaAoe.rows) or []) %}
  {% set key = ((item.url or "") ~ "|" ~ (item.title or "")) | trim %}
  {% if key != "|" and (uniquePresentationKeys.indexOf(key) == -1) %}
    {% set _ = (uniquePresentationKeys.push(key), null) %}
  {% endif %}
{% endfor %}
{% set heroPresentationCount = uniquePresentationKeys | length %}
{% set heroPublicationCount = (researchfi or []) | length %}
{% set heroOpinionCount = (collections.pub_mielipide or []) | length %}
{% set heroSpeechCount = (collections.pub_puhe or []) | length %}
{% set heroStats = [
    { count: heroPresentationCount, label: "Esitystä tai oppimateriaalia", href: "/esitykset/" },
    { count: heroPublicationCount, label: "julkaisua", href: "/julkaisut/" },
    { count: heroOpinionCount, label: "mielipidekirjoitusta", href: "/kynasta/#mielipiteet" },
    { count: heroSpeechCount, label: "puheenvuoroa", href: "/kynasta/#puheet" },
    { count: 100, label: "ohjattua opinnäytettä", href: "/opinnaytteet/" }
] %}

<section class="section-flush home-hero-section" id="heroSection">
    <div class="home-hero-backdrop">
        <div class="container home-hero-content">
            <p class="home-hero-eyebrow mb-2">Jari Laru</p>
            <h1 class="home-hero-title mb-3">Koulutan ja tutkin, miten tekoäly ja oppimisteknologia toimivat koulun arjessa.</h1>
            <div class="home-hero-roles mb-2">
                <a class="home-hero-role-item" href="/tyoni-yliopistonlehtorina/">Yliopistonlehtori</a>
                <a class="home-hero-role-item" href="/larux-tmi/">Kouluttaja (Larux t:mi)</a>
                <a class="home-hero-role-item" href="/politiikka/">Kuntapäättäjä</a>
            </div>
        </div>
    </div>
    <div class="home-hero-wave" aria-hidden="true">
        <svg viewBox="0 0 1440 60" preserveAspectRatio="none" xmlns="http://www.w3.org/2000/svg">
            <path d="M0,30 C240,60 480,0 720,30 C960,60 1200,0 1440,30 L1440,60 L0,60 Z"/>
        </svg>
    </div>
</section>

{# ── Intro: tiivistys + päätoiminnot ──────────────────────────────────── #}
<section class="py-5 home-intro-section">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8 text-center">
                <p class="home-intro-text">Yliopistonlehtori, kouluttaja ja kuntapäättäjä. Tutkin ja koulutan miten tekoäly ja oppimisteknologia toimivat koulun arjessa — ja vaikutan päätöksentekoon, jotta tutkimusnäyttö löytää tiensä luokkahuoneeseen.</p>
                <div class="home-intro-actions">
                    <a class="btn btn-primary rounded-pill px-4" href="/yhteystiedot/">Pyydä tarjous</a>
                    <a class="btn btn-outline-secondary rounded-pill px-4" href="/tietoa/">Tutustu taustaani</a>
                </div>
            </div>
        </div>
    </div>
</section>

{# ── Kolmipolku: kenelle tämä on? ─────────────────────────────────────── #}
<section class="py-5 home-paths-section">
    <div class="container">
        <p class="text-uppercase fw-bold text-center mb-4 home-paths-label">Mistä lähdet liikkeelle?</p>
        <div class="row g-4">

            <div class="col-md-4">
                <a class="home-path-card" href="/larux-tmi/">
                    <div class="home-path-icon">
                        <i class="bi bi-person-workspace" aria-hidden="true"></i>
                    </div>
                    <h2 class="home-path-title">Koulutuksen tilaajalle</h2>
                    <p class="home-path-desc">Tarvitsetko kouluttajan tai keynote-puhujan? Käytännönläheiset koulutukset tekoälystä, digipedagogiikasta ja oppimisesta organisaatioille ja oppilaitoksille.</p>
                    <span class="home-path-cta">Koulutuspalvelut ja tarjouspyyntö →</span>
                </a>
            </div>

            <div class="col-md-4">
                <a class="home-path-card" href="/tutkimus/">
                    <div class="home-path-icon">
                        <i class="bi bi-journal-text" aria-hidden="true"></i>
                    </div>
                    <h2 class="home-path-title">Tutkijalle &amp; kollegalle</h2>
                    <p class="home-path-desc">Vertaisarvioidut julkaisut, hankeyhteistyö ja tutkimusagenda teknologiatuetusta oppimisesta ja tekoälykasvatuksesta.</p>
                    <span class="home-path-cta">Tutkimussivulle →</span>
                </a>
            </div>

            <div class="col-md-4">
                <a class="home-path-card" href="/politiikka/">
                    <div class="home-path-icon">
                        <i class="bi bi-building-check" aria-hidden="true"></i>
                    </div>
                    <h2 class="home-path-title">Oululaiselle &amp; äänestäjälle</h2>
                    <p class="home-path-desc">Varavaltuutettu ja sivistyslautakunnan jäsen Oulussa (Kokoomus). Seuraa valtuustoaloitteita, puheenvuoroja ja mielipidekirjoituksia — painoalueina kouluverkko, hallinnon avoimuus ja kaupunkialueiden tasapuolinen kehitys.</p>
                    <span class="home-path-cta">Politiikkasivu →</span>
                </a>
            </div>

        </div>
    </div>
</section>

{# ── Miksi tämä työ on tärkeää? ────────────────────────────────────────── #}
<section class="py-5 bg-body-tertiary home-challenges-section">
    <div class="container">
        <div class="row justify-content-center mb-5">
            <div class="col-lg-7 text-center">
                <h2 class="fw-bold mb-2">Miksi tämä työ on tärkeää?</h2>
                <p class="home-challenges-lead">Kolme toisiinsa kytkeytyvää haastetta, joihin vastaan tutkimuksella, koulutuksella ja yhteiskunnallisella vaikuttamisella.</p>
            </div>
        </div>
        <div class="row g-4">

            <div class="col-md-4">
                <div class="home-challenge-card">
                    <div class="home-challenge-num">01</div>
                    <h3 class="home-challenge-title">Tekoäly muuttaa opettamista nopeammin kuin koulut ehtivät sopeutua</h3>
                    <p class="home-challenge-text">Oppilaitokset etsivät käytännön ratkaisuja tekoälyn integrointiin, mutta tutkimusnäyttöön perustuva tuki ja osaaminen puuttuvat liian usein.</p>
                </div>
            </div>

            <div class="col-md-4">
                <div class="home-challenge-card">
                    <div class="home-challenge-num">02</div>
                    <h3 class="home-challenge-title">Tutkimustieto jää liian usein vain tutkijoiden pöytälaatikoihin</h3>
                    <p class="home-challenge-text">Tieteellinen tieto ja käytännön luokkahuonetyö eivät kohtaa riittävästi. Sillä on väliä, kuka välittää tietoa ja miten.</p>
                </div>
            </div>

            <div class="col-md-4">
                <div class="home-challenge-card">
                    <div class="home-challenge-num">03</div>
                    <h3 class="home-challenge-title">Koulutuspäätökset tehdään liian harvoin tutkimusnäytön pohjalta</h3>
                    <p class="home-challenge-text">Digitalisaatiota koskevat kuntapäätökset vaativat asiantuntemusta, jota liian harvoilla päättäjillä on — ja johon voi vaikuttaa.</p>
                </div>
            </div>

        </div>
    </div>
</section>

{# ── KPI-kaista: vaikuttavuusluvut ────────────────────────────────────── #}
<section class="home-stats-section" aria-label="Vaikuttavuusluvut">
    <div class="container">
        <dl class="home-stats-grid">
            {% for stat in heroStats | sort(true, false, "count") %}
            <div class="home-stat">
                <dd class="home-stat-value"><a href="{{ stat.href }}">{{ stat.count }}</a></dd>
                <dt class="home-stat-label">{{ stat.label }}</dt>
            </div>
            {% endfor %}
        </dl>
    </div>
</section>

<section class="py-5 home-overview-section">
    <div class="container">
        <div class="row g-4 g-xl-5">

            {# Kortti 1: Yliopistonlehtori #}
            <div class="col-md-6 col-xl-3 home-overview-col">
                <div class="d-flex flex-column h-100">
                    <p class="home-overview-eyebrow">Yliopisto</p>
                    <h2 class="h5 fw-bold mb-2 home-overview-title">
                        <i class="bi bi-mortarboard me-2 opacity-75"></i>Yliopistonlehtori
                    </h2>
                    <div class="home-overview-lead-wrap mb-3">
                        <p class="small mb-0 home-overview-lead">Teknologiatuetun oppimisen yliopistonlehtori, Kasvatustieteiden tiedekunta, Oulun yliopisto. Mukana STN:n rahoittamassa Generation AI -hankkeessa — tekoälykasvatusta esi- ja perusopetukseen.</p>
                        <div class="home-overview-lead-fade"></div>
                    </div>
                    <div class="home-overview-kpis mb-3">
                        <div class="home-overview-kpi">
                            <span class="home-overview-kpi-value">100+</span>
                            <span class="home-overview-kpi-label">opinnäytettä</span>
                        </div>
                        <div class="home-overview-kpi">
                            <span class="home-overview-kpi-value">15+</span>
                            <span class="home-overview-kpi-label">v. kokemus</span>
                        </div>
                    </div>
                    <div class="home-overview-items mb-3">
                        {% for post in collections.blog | take(3) %}
                        <a href="{{ post.url }}" class="home-overview-item">
                            <p class="home-overview-item-title">{{ post.data.title }}</p>
                            <p class="home-overview-item-meta">Kirjoitus · {{ post.date | dateFormat }}</p>
                        </a>
                        {% endfor %}
                        <div class="home-overview-items-fade"></div>
                    </div>
                    <div class="mt-auto pt-2">
                        <a href="/tyoni-yliopistonlehtorina/" class="btn btn-outline-secondary btn-sm rounded-pill px-3">Lue lisää <i class="bi bi-arrow-right"></i></a>
                    </div>
                </div>
            </div>

            {# Kortti 2: Kouluttaja & puhuja #}
            <div class="col-md-6 col-xl-3 home-overview-col">
                <div class="d-flex flex-column h-100">
                    <p class="home-overview-eyebrow">Koulutuspalvelut · Larux t:mi</p>
                    <h2 class="h5 fw-bold mb-2 home-overview-title">
                        <i class="bi bi-mic me-2 opacity-75"></i>Kouluttaja &amp; puhuja
                    </h2>
                    <div class="home-overview-lead-wrap mb-3">
                        <p class="small mb-0 home-overview-lead">Käytännönläheisiä koulutuksia opettajille, rehtoreille ja organisaatioille — tekoälyn soveltamisesta kouluarjessa keynote-puheenvuoroihin ja työpajoihin.</p>
                        <div class="home-overview-lead-fade"></div>
                    </div>
                    <div class="home-overview-kpis mb-3">
                        <div class="home-overview-kpi">
                            <span class="home-overview-kpi-value">{{ heroPresentationCount }}</span>
                            <span class="home-overview-kpi-label">esitystä</span>
                        </div>
                        <div class="home-overview-kpi">
                            <span class="home-overview-kpi-value">50+</span>
                            <span class="home-overview-kpi-label">koulutusta</span>
                        </div>
                    </div>
                    <div class="home-overview-items mb-3">
                        {% for row in (canva.tableRows or []) | take(3) %}
                        <a href="{{ row.url }}" target="_blank" rel="noopener noreferrer" class="home-overview-item">
                            <p class="home-overview-item-title">{{ row.title }}</p>
                            <p class="home-overview-item-meta">{% if row.location %}{{ row.location }} · {% endif %}{{ row.date | dateFormat }}</p>
                        </a>
                        {% endfor %}
                        <div class="home-overview-items-fade"></div>
                    </div>
                    <div class="d-flex flex-wrap gap-2 mt-auto pt-2">
                        <a href="/larux-tmi/" class="btn btn-primary btn-sm rounded-pill px-3">Tilaa koulutus</a>
                        <a href="/esitykset/" class="btn btn-outline-secondary btn-sm rounded-pill px-3">Kaikki esitykset</a>
                    </div>
                </div>
            </div>

            {# Kortti 3: Julkaisut #}
            <div class="col-md-6 col-xl-3 home-overview-col">
                <div class="d-flex flex-column h-100">
                    <p class="home-overview-eyebrow">Tiede &amp; tutkimus</p>
                    <h2 class="h5 fw-bold mb-2 home-overview-title">
                        <i class="bi bi-journal-text me-2 opacity-75"></i>Julkaisut
                    </h2>
                    <div class="home-overview-lead-wrap mb-3">
                        <p class="small mb-0 home-overview-lead">Vertaisarvioituja artikkeleita ja konferenssijulkaisuja teknologiatuetusta oppimisesta, tekoälykasvatuksesta ja digipedagogiikasta.</p>
                        <div class="home-overview-lead-fade"></div>
                    </div>
                    <div class="home-overview-kpis mb-3">
                        <div class="home-overview-kpi">
                            <span class="home-overview-kpi-value">{{ heroPublicationCount }}</span>
                            <span class="home-overview-kpi-label">julkaisua</span>
                        </div>
                        <div class="home-overview-kpi">
                            <span class="home-overview-kpi-value">{{ semanticscholar.metrics.totalCitations or "600+" }}</span>
                            <span class="home-overview-kpi-label">viittausta</span>
                        </div>
                    </div>
                    <div class="home-overview-items mb-3">
                        {% for pub in researchfi.slice(0, 3) %}
                        {% set pubLink = pub.doiUrl or pub.url %}
                        <a href="{{ pubLink or '/julkaisut/' }}" {% if pubLink %}target="_blank" rel="noopener noreferrer"{% endif %} class="home-overview-item">
                            <p class="home-overview-item-title">{{ pub.title | truncate(85) }}</p>
                            <p class="home-overview-item-meta">{% if pub.authors %}{{ pub.authors | truncate(40) }} · {% endif %}{{ pub.year }}</p>
                        </a>
                        {% endfor %}
                        <div class="home-overview-items-fade"></div>
                    </div>
                    <div class="mt-auto pt-2">
                        <a href="/julkaisut/" class="btn btn-outline-secondary btn-sm rounded-pill px-3">Kaikki julkaisut</a>
                    </div>
                </div>
            </div>

            {# Kortti 4: Politiikka #}
            <div class="col-md-6 col-xl-3 home-overview-col">
                <div class="d-flex flex-column h-100">
                    <p class="home-overview-eyebrow">Yhteiskunnallinen vaikuttaminen</p>
                    <h2 class="h5 fw-bold mb-2 home-overview-title">
                        <i class="bi bi-building me-2 opacity-75"></i>Politiikka
                    </h2>
                    <div class="home-overview-lead-wrap mb-3">
                        <p class="small mb-0 home-overview-lead">Toisen kauden kaupunginvaltuutettu Oulussa (Kokoomus). Erityisaiheina sivistys, varhaiskasvatuksen laatu ja koulutuksen digitalisaatio.</p>
                        <div class="home-overview-lead-fade"></div>
                    </div>
                    <div class="home-overview-kpis mb-3">
                        <div class="home-overview-kpi">
                            <span class="home-overview-kpi-value">{{ heroOpinionCount }}</span>
                            <span class="home-overview-kpi-label">mielipidettä</span>
                        </div>
                        <div class="home-overview-kpi">
                            <span class="home-overview-kpi-value">{{ heroSpeechCount }}</span>
                            <span class="home-overview-kpi-label">puheenvuoroa</span>
                        </div>
                    </div>
                    <div class="home-overview-items mb-3">
                        {% set polItems = [] %}
                        {% for post in collections.politicsAndOpinions %}
                            {% if post.fileSlug != "etusivu" and polItems.length < 3 %}
                                {% set _ = (polItems.push(post), null) %}
                            {% endif %}
                        {% endfor %}
                        {% for post in polItems %}
                        <a href="{{ post.url }}" class="home-overview-item">
                            <p class="home-overview-item-title">{{ post.data.title }}</p>
                            <p class="home-overview-item-meta">{{ post.data.type | capitalize or "Kirjoitus" }} · {{ post.date | dateFormat }}</p>
                        </a>
                        {% endfor %}
                        <div class="home-overview-items-fade"></div>
                    </div>
                    <div class="d-flex flex-wrap gap-2 mt-auto pt-2">
                        <a href="/politiikka/" class="btn btn-outline-secondary btn-sm rounded-pill px-3">Politiikkasivu</a>
                        <a href="/kynasta/" class="btn btn-outline-secondary btn-sm rounded-pill px-3">Kynästä</a>
                    </div>
                </div>
            </div>

        </div>
    </div>
</section>

<section class="py-5 bg-body-tertiary home-testimonials-section">
    <div class="container">
        <p class="text-uppercase fw-bold text-center mb-4 home-testimonials-label">Palautetta koulutuksista</p>
        <div class="row g-4 justify-content-center">
            <div class="col-md-5">
                <blockquote class="home-quote">
                    <p>"Ihan fiiliksissä vieläkin setistäsi. Yhteyden katkeaminen ei latistanut, vaan porukka alkoi heti reflektoida kuultua."</p>
                    <footer class="home-quote-footer">Henkilöstökoulutus</footer>
                </blockquote>
            </div>
            <div class="col-md-5">
                <blockquote class="home-quote">
                    <p>"Palaute kyselyn perusteella olit osallistujien mielestä päivän paras puhuja."</p>
                    <footer class="home-quote-footer">Maksullinen seminaari</footer>
                </blockquote>
            </div>
        </div>
    </div>
</section>

{# ── Viimeisin sisältö -ticker ─────────────────────────────────────────── #}

{# Yhdistetään kaikki sisältötyypit ja lajitellaan uusin ensin #}
{% set allRecentItems = [] %}

{% for post in collections.blog %}
  {% set item = { sortTs: post.date | toTimestamp, type: "blog", dateLabel: post.date | dateFormat, title: post.data.title, href: post.url, desc: post.data.description, isExternal: false } %}
  {% set _ = (allRecentItems.push(item), null) %}
{% endfor %}

{# canva.tableRows käytetään presentations-sijaan: siinä on paikalliset thumbnail-polut #}
{% for row in (canva.tableRows or []) %}
  {% set item = { sortTs: row.date | toTimestamp, type: "pres", dateLabel: row.date | dateFormat, title: row.title, href: row.url, desc: row.location, thumbnail: row.thumbnail, isExternal: true } %}
  {% set _ = (allRecentItems.push(item), null) %}
{% endfor %}

{% for pub in (researchfi or []) %}
  {% set pubDateStr = pub.year ~ "-12-31" %}
  {% set item = { sortTs: pubDateStr | toTimestamp, type: "pub", dateLabel: pub.year, title: pub.title, href: pub.doiUrl or pub.url or "", authors: pub.authors, journal: pub.journal, typeShort: pub.typeShort, isExternal: true, hasDoi: pub.doiUrl } %}
  {% set _ = (allRecentItems.push(item), null) %}
{% endfor %}

{% set allRecentSorted = allRecentItems | sort(true, false, "sortTs") | take(18) %}

<section class="py-5 home-recent-section">
    <div class="container">
        <div class="d-flex align-items-baseline justify-content-between mb-4">
            <p class="text-uppercase fw-bold mb-0 home-recent-label">Viimeisin sisältö</p>
            <div class="d-flex gap-3">
                <a href="/blogi/" class="home-recent-more">Kirjoitukset →</a>
                <a href="/esitykset/" class="home-recent-more">Esitykset →</a>
                <a href="/julkaisut/" class="home-recent-more">Julkaisut →</a>
            </div>
        </div>
    </div>
    <div class="ticker-wrapper home-ticker-wrapper" id="recentTicker">
        <div class="ticker-track">

            {%- macro tickerCard(item) %}
            <div class="home-ticker-item home-ticker-item--{{ item.type }}">

                {# Yläosa: sisältö (kuva / viiteteksti / kuvaus) #}
                <div class="home-ticker-top">
                    {%- if item.type == "pres" and item.thumbnail %}
                    <img src="{{ item.thumbnail }}" alt="" loading="lazy" decoding="async">
                    {%- elif item.type == "pub" %}
                    <p class="home-apa7">
                        {%- if item.authors %}<span class="home-apa7-authors">{{ item.authors }}.</span> {% endif -%}
                        ({{ item.dateLabel }}).
                        <em>{{ item.title }}</em>.
                        {%- if item.journal %} <span class="home-apa7-journal">{{ item.journal }}</span>.{% endif %}
                        {%- if item.hasDoi %} <span class="home-apa7-doi">{{ item.hasDoi }}</span>{% endif %}
                    </p>
                    {%- elif item.desc %}
                    <p class="home-ticker-top-text">{{ item.desc }}</p>
                    {%- endif %}
                    <div class="home-ticker-top-fade"></div>
                </div>

                {# Keskiosa: badge + aikaleima #}
                <div class="home-ticker-meta">
                    <span class="home-ticker-badge home-ticker-badge--{{ item.type }}">
                        {%- if item.type == "blog" %}Kirjoitus{% elif item.type == "pres" %}Esitys{% else %}Julkaisu{% endif -%}
                    </span>
                    <span class="home-ticker-date">&nbsp;·&nbsp;{{ item.dateLabel }}</span>
                    {%- if item.typeShort %}&nbsp;<span class="home-apa7-type">{{ item.typeShort }}</span>{% endif %}
                </div>

                {# Alaosa: otsikko, max 2 riviä #}
                <div class="home-ticker-title-wrap">
                    {%- if item.href %}
                    <a class="home-ticker-title" href="{{ item.href }}"{% if item.isExternal %} target="_blank" rel="noopener"{% endif %}>{{ item.title }}</a>
                    {%- else %}
                    <span class="home-ticker-title">{{ item.title }}</span>
                    {%- endif %}
                </div>

            </div>
            {%- endmacro %}

            {# ── Setti 1 (alkuperäinen) ─── #}
            <div class="ticker-content">
                {% for item in allRecentSorted %}{{ tickerCard(item) }}{% endfor %}
            </div>

            {# ── Setti 2 (klooni saumatonta toistoa varten) ─── #}
            <div class="ticker-content" aria-hidden="true" inert>
                {% for item in allRecentSorted %}{{ tickerCard(item) }}{% endfor %}
            </div>

        </div>
    </div>
</section>

<script>
document.addEventListener("DOMContentLoaded", function () {
    const ticker = document.getElementById("recentTicker");
    if (!ticker) return;
    const track = ticker.querySelector(".ticker-track");
    const tickerContent = ticker.querySelector(".ticker-content");
    if (!track || !tickerContent) return;

    let direction = -1;
    const speed = 0.4;
    let currentX = 0;
    let animationId;
    let isDragging = false;
    let prevX = 0;

    track.style.animation = "none";

    function animate() {
        const setWidth = tickerContent.offsetWidth;
        currentX += direction * speed;
        if (currentX <= -setWidth) currentX += setWidth;
        else if (currentX >= 0) currentX -= setWidth;
        track.style.transform = `translateX(${currentX}px)`;
        animationId = requestAnimationFrame(animate);
    }

    animationId = requestAnimationFrame(animate);

    ticker.addEventListener("mouseenter", () => cancelAnimationFrame(animationId));
    ticker.addEventListener("mouseleave", () => {
        isDragging = false;
        ticker.style.cursor = "";
        animationId = requestAnimationFrame(animate);
    });
    ticker.addEventListener("mousedown", (e) => {
        isDragging = true;
        prevX = e.clientX;
        ticker.style.cursor = "grabbing";
        cancelAnimationFrame(animationId);
    });
    ticker.addEventListener("mousemove", (e) => {
        if (!isDragging) return;
        const dx = e.clientX - prevX;
        prevX = e.clientX;
        direction = dx > 0 ? 1 : -1;
        currentX += dx;
        const setWidth = tickerContent.offsetWidth;
        if (currentX <= -setWidth) currentX += setWidth;
        else if (currentX >= 0) currentX -= setWidth;
        track.style.transform = `translateX(${currentX}px)`;
    });
    ticker.addEventListener("mouseup", () => {
        isDragging = false;
        ticker.style.cursor = "ew-resize";
    });
});
</script>

<section class="py-5 bg-body-tertiary home-closing-section" style="border-top: 1px solid var(--bs-border-color-translucent);">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-9">
                <div class="home-closing-panel text-center">
                    <p class="text-uppercase text-muted fw-bold mb-2">Seuraava askel</p>
                    <h2 class="fw-bold mb-3">Tarvitsetko kouluttajan, puhujan tai tutkimukseen nojaavan asiantuntijan?</h2>
                    <p class="mb-4">Sivulla esitellyt teemat jatkuvat käytännön yhteistyössä: koulutuksina, puheenvuoroina, hanketukena ja kehittämistyön sparrauksena.</p>
                    <div class="home-closing-actions d-flex flex-wrap justify-content-center gap-2 mb-3">
                        <a class="btn btn-primary rounded-pill px-4" href="/yhteystiedot/">Pyydä tarjous</a>
                        <a class="btn btn-read-more btn-sm rounded-pill px-3" href="/larux-tmi/">Katso palvelut</a>
                        <a class="btn btn-outline-secondary btn-sm rounded-pill px-3" href="/tietoa/">Tutustu taustaani</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
